import Insertion from "../src/insertion-sort";

describe('Insertion', () => {
  
  beforeEach(() => {
    const unsortedArr = [9, 5, 7, 3, 15, 12];
  })

  test('should assess length of input array', () => {
    expect(thing).toEqual([]);
  });

});